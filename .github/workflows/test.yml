name: Test
on:
  workflow_dispatch:
    inputs:
      value:
        type: text
        required: false
      
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "value: ${{ github.ref_name }}"
