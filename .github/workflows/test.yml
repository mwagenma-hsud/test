name: Test
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          #yq '.jobs.test.runs-on' .github/workflows/test.yml
          #yq r .github/workflows/test.yml '.global' -ppv
          #env $(./yq r .github/workflows/env.yml '.global' -ppv | sed 's/: /=/' | xargs )
          ./yq r .github/workflows/env.yml '.global' -ppv | sed 's/: /=/' | xargs
